CREATE TABLE owner (
    id_owner            INTEGER NOT NULL,
    name_owner          VARCHAR(50) NOT NULL,
    lastname_owner      VARCHAR(50) NOT NULL,
    phonenumber_owner   VARCHAR(50) NOT NULL,
    email_owner         VARCHAR(70) NOT NULL,
    password_owner      VARCHAR(50) NOT NULL
)

go